<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence" 
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
             version="2.1" 
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence 
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd">
    
    <persistence-unit name="ORACLE_FIAP" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        
        <class>com.fiap.brinquedos.model.Brinquedo</class>
        
        <properties>
            <!-- Exibe as queries executadas no banco de dados -->
            <property name="hibernate.show_sql" value="true"/>
            
            <!-- Formatar as queries que estão sendo exibidas -->
            <property name="hibernate.format_sql" value="true"/>
            
            <!-- Definir o que será realizado no banco
                update -> atualizar/criar as tabelas de acordo com as classes (mantém dados existentes)
                create -> apaga e cria as tabelas de acordo com as classes (perde dados existentes)
                create-drop -> cria as tabelas ao iniciar e apaga ao finalizar a aplicação
                validate -> valida se as tabelas estão de acordo com as classes (não faz alterações)
                none -> não faz nenhuma alteração no banco de dados -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            
            <!-- Dialeto, responsável por gerar o SQL nativo -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.Oracle12cDialect"/>
            
            <!-- Driver do banco -->
            <property name="javax.persistence.jdbc.driver" value="oracle.jdbc.OracleDriver"/>
            
            <!-- Usuário do banco -->
            <property name="javax.persistence.jdbc.user" value="RMxxxxxx"/>
            
            <!-- Senha do banco -->
            <property name="javax.persistence.jdbc.password" value="xxxxxx"/>
            
            <!-- URL do banco -->
            <property name="javax.persistence.jdbc.url" value="jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL"/>
        </properties>
    </persistence-unit>
</persistence> 